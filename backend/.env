PORT=4000


### What is a `.env` file?

A **`.env` file** is a file used to store **environment variables**—configuration values that your application needs but that you usually don't want to hard-code directly into your source code.
